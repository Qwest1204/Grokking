# Грокаем нейросети

Этот проект воспроизводит эксперименты из оригинальной статьи "Grokking: Generalization Beyond Overfitting" на задаче модульного сложения с использованием трансформерной архитектуры.

## 🧠 Феномен грокания

"Грокание" (grokking) — это явление, когда нейронная сеть сначала достигает 100% точности на обучающей выборке (переобучается), а затем после значительного количества дополнительных эпох внезапно достигает почти идеальной точности на тестовой выборке. Этот эффект демонстрирует, что модели могут учиться обобщать даже после достижения минимума обучающего лосса.

## 🏗️ Архитектура модели

Используется упрощенный трансформер с одним слоем внимания:

### Основные компоненты:
1. **InputEmbedding** - преобразование токенов в векторы
2. **PositionalEncoding** - позиционное кодирование
3. **MHA (Multi-Head Attention)** - механизм внимания с 4 головами
4. **MLP** - двухслойный персептрон с активацией ReLU
5. **LayerNormalization** - слой нормализации
6. **ProjectionLayer** - проекция на выходной словарь

### Параметры модели:
- `d_model = 64` (размерность модели)
- `num_heads = 4` (количество голов внимания)
- `d_ff = 1024` (размерность скрытого слоя MLP)
- `dropout = 0.05`
- `vocab_size = 98` (числа 0-96 + токен '=')

## 📊 Датасет

**ModularAdditionDataset** - генерирует все возможные пары (x, y) для операции:

(x + y) mod p, где p = 97


### Формат данных:
- **Вход**: последовательность из 3 токенов `[x, y, =]`
- **Цель**: результат `(x + y) mod 97`

### Разделение данных:
- **Обучающая выборка**: 500 примеров (~5% от общего количества)
- **Тестовая выборка**: 8909 примеров (остальные 95%)

## ⚙️ Экспериментальная установка

### Гиперпараметры обучения:
- **Оптимизатор**: AdamW
- **Learning rate**: 0.003
- **Weight decay**: 0.001
- **Batch size**: 16
- **Планировщик**: CosineAnnealingLR с T_max=10000
- **Эпохи**: 10000

### Мониторинг:
- TensorBoard для логирования метрик
- Визуализация лосс-ландшафта каждые 10 эпох
- Логирование весов и градиентов
- Визуализация карт внимания

## 📈 Визуализации

Проект включает несколько видов визуализаций:

### 1. Loss Landscape (3D и 2D)
Визуализация поверхности функции потерь вокруг текущих параметров модели:
- 3D поверхность с контурными линиями
- 2D тепловая карта с уровнями

### 2. Карты внимания
Визуализация механизма внимания для каждой головы

### 3. Метрики обучения:
- Loss (train/test)
- Accuracy (train/test)
- Нормы градиентов
- Разрывы между train/test метриками

## 📊 Результаты

### Ожидаемое поведение (согласно оригинальной статье):
1. **Фаза 1 (100-1000 эпох)**: Быстрое уменьшение train loss, переобучение
2. **Фаза 2 (1000-4000 эпох)**: Плато, где train accuracy = 100%, test accuracy низкая
3. **Фаза 3 (4000+ эпох)**: Внезапный рост test accuracy до ~100%

### Ключевые метрики грокания:
- **Training accuracy**: достигает 100% быстро
- **Test accuracy**: остается низкой долгое время, затем резко растет
- **Loss gap**: разница между train и test loss уменьшается при грокании

## 🚀 Запуск кода

### Требования:
```bash
pip install -r requirements.txt
```

### Запуск обучения:
```bash
python train.py
```

### Просмотр результатов в TensorBoard:
```bash
tensorboard --logdir=runs/
```

## 🔍 Ключевые наблюдения

1. **Важность регуляризации**

Weight decay (0.001) критически важен для проявления грокания
Без регуляризации модель остается в состоянии переобучения
2. **Роль размера модели**

Слишком маленькие модели не проявляют грокание
Слишком большие модели могут не показывать четкий переход
3. **Влияние размера обучающей выборки**

Маленькая обучающая выборка (5%) способствует проявлению грокания
С большей выборкой эффект менее выражен
4. **Loss landscape анализ**

В начале обучения: острые минимумы
При грокании: переход к более плоским минимумам
Плоские минимумы лучше обобщают

### Всё я устал писать этот понос, чекайте код